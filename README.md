# ChipGPT Frontend

<div align="center">
  <img src="./docs/preview.png" alt="ChipGPT Interface Preview" width="800"/>
</div>

## Описание

**ChipGPT** — веб-интерфейс для AI-ассистента, специализирующегося на генерации, отладке и верификации HDL-кода (Verilog, VHDL, SystemVerilog).

Интерфейс выполнен в минималистичном стиле, вдохновленном [Qwen AI](https://chat.qwen.ai), с фокусом на контенте и удобной двухпанельной компоновкой при работе с кодом.

## Основные возможности

- 🤖 **AI-чат** с поддержкой стриминга ответов
- 💻 **Monaco Editor** с подсветкой синтаксиса для Verilog/VHDL/SystemVerilog
- 📝 **Markdown + LaTeX** рендеринг для формул и документации
- 🔌 **Визуализация pinout** модулей (SVG-схемы портов)
- 📚 **История чатов** с группировкой по времени
- 🎨 **Три режима работы**:
  - Zero State — приветственный экран с быстрыми промптами
  - Chat Mode — стандартный режим чата
  - Workspace Mode — двухпанельный режим (чат + редактор кода)

## Технологический стек

- **Framework**: Next.js 14+ (App Router)
- **State Management**: Zustand
- **Styling**: Tailwind CSS
- **Code Editor**: Monaco Editor (движок VS Code)
- **Markdown**: react-markdown + rehype-highlight + rehype-katex
- **API**: Server-Sent Events (SSE) для стриминга

## Установка и запуск

### Требования

- Node.js 18+ 
- npm или yarn

### Шаги установки

```bash
# Клонировать репозиторий
git clone https://github.com/misheneva/ChipGPT_front.git
cd ChipGPT_front

# Установить зависимости
npm install

# Запустить dev-сервер
npm run dev
```

Приложение будет доступно по адресу [http://localhost:3000](http://localhost:3000)

### Другие команды

```bash
# Сборка для production
npm run build

# Запуск production-сборки
npm start

# Линтинг
npm run lint
```

## Структура проекта

```
ChipGPT_front/
├── app/                    # Next.js App Router
│   ├── api/               # API routes (SSE endpoint)
│   ├── layout.tsx         # Root layout
│   ├── page.tsx           # Главная страница
│   └── globals.css        # Глобальные стили
├── components/
│   ├── chat/             # Компоненты чата
│   ├── editor/           # Monaco Editor workspace
│   ├── input/            # Поле ввода
│   ├── sidebar/          # Боковая панель
│   └── common/           # Общие компоненты (PinoutVisualizer)
├── lib/
│   ├── api.ts            # API клиент (SSE)
│   ├── store.ts          # Zustand store
│   ├── types.ts          # TypeScript типы
│   └── utils.ts          # Утилиты
└── docs/                 # Документация и изображения
```

## Особенности интерфейса

### Цветовая палитра

- **Background**: `#FFFFFF` (Light), `#1E1E1E` (Dark)
- **Primary Action**: `#615CED` (фиолетово-синий)
- **Surface**: `#F7F7F8` (сообщения пользователя), `#FFFFFF` (ответы AI)

### Горячие клавиши

- `Enter` — отправить сообщение
- `Shift+Enter` — новая строка в поле ввода

### Режим Workspace

При генерации HDL-кода автоматически открывается правая панель с Monaco Editor, где доступны:
- Подсветка синтаксиса
- Нумерация строк
- Копирование кода
- Скачивание файла (.v/.sv/.vhd)
- Генерация testbench
- Визуализация pinout модуля

## API

### Endpoint: `/api/chat/completions`

**Request:**
```json
{
  "messages": [...],
  "model": "qwen-coder-verilog",
  "stream": true
}
```

**Response:** Server-Sent Events (SSE) поток с токенами ответа

## Разработка

Проект использует TypeScript для типобезопасности. Все компоненты написаны с использованием React Server Components и Client Components где необходимо.

### Добавление новых языков в Monaco

Monaco Editor поддерживает Verilog, VHDL и SystemVerilog из коробки. Для добавления дополнительных языков см. [документацию Monaco](https://microsoft.github.io/monaco-editor/).

## Лицензия

MIT

## Автор

[misheneva](https://github.com/misheneva)

---

**Примечание**: Для работы с реальным AI-бэкендом необходимо настроить endpoint в `lib/api.ts` и обновить `app/api/chat/completions/route.ts` для проксирования запросов к вашему серверу.

